<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
         <% if(error.status == 404){ %>
				<h2><%= error.status %> <i class="icon-line-awesome-question-circle"></i></h2>
				<p>We're sorry, but the page you were looking for doesn't exist</p>
				<% } else{ %>
				<h2><%= error.status %> <i class="icon-line-awesome-question-circle"></i></h2>
				<p><%= error.message %></p>
				<p class="small"><%= error.stack %></p>
				<% } %>
    </body>
</html>